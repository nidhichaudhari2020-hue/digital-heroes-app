import { redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server"; import { AdminClient } from "@/components/admin-client"; import { AppHeader } from "@/components/app-header";
export default async function Admin(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/auth");const {data:profile}=await supabase.from("profiles").select("role").eq("id",user.id).single();if(profile?.role!=="admin")redirect("/dashboard");return <div className="app-shell"><AppHeader admin/><main className="app-container"><div className="app-heading"><div><p className="eyebrow">ADMIN CONTROL ROOM</p><h1>Platform <em>overview.</em></h1></div><span className="session-pill session-pill--admin">● ADMIN SESSION</span></div><AdminClient /></main></div>}
