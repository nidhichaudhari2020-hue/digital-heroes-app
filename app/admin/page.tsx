import { redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server"; import { AdminClient } from "@/components/admin-client";
export default async function Admin(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/auth");const {data:profile}=await supabase.from("profiles").select("role").eq("id",user.id).single();if(profile?.role!=="admin")redirect("/dashboard");return <main className="shell"><div className="page-title"><div><div className="eyebrow">ADMIN CONTROL ROOM</div><h1>Platform <em>overview.</em></h1></div><span className="status">● ADMIN SESSION</span></div><AdminClient /></main>}
