import Link from "next/link";
import { SiteFooter, SiteHeader } from "@/components/site-header";
const tiers=[['5 matches','40% + rollover jackpot','A five-match prize carries forward when nobody claims it.'],['4 matches','35% of the monthly pool','Every winner shares their tier equally.'],['3 matches','25% of the monthly pool','A rewarding finish for a strong match.']];
export default function DrawPage(){return <><SiteHeader/><main><section className="draw-hero"><div><p className="eyebrow eyebrow--light">THE MONTHLY IMPACT DRAW</p><h1>Good form.<br/><em>Real possibility.</em></h1><p>Every active member has a place in the monthly draw. Your subscription helps grow the pool, and a share always goes towards the causes our members choose.</p><Link className="button button--light" href="/auth">Join the next draw</Link></div><div className="draw-orbital" aria-label="Example draw numbers">{[7,14,22,31,44].map((n,index)=><span className={`orbital-number n${index+1}`} key={n}>{String(n).padStart(2,'0')}</span>)}<i/></div></section><section className="prize-explainer"><div><p className="eyebrow">CLEAR BY DESIGN</p><h2>Every rupee has<br/>a <em>purpose.</em></h2></div><div className="tier-list">{tiers.map(([match,share,copy])=><article key={match}><span>{match}</span><b>{share}</b><p>{copy}</p></article>)}</div></section><section className="rules-strip"><div><b>01</b><p>Draws are run monthly by an administrator.</p></div><div><b>02</b><p>Simulations are reviewed before public results are published.</p></div><div><b>03</b><p>Winners verify their score evidence before payment.</p></div></section></main><SiteFooter/></>}
